@model IEnumerable<FlowCaseTracking.Models.User>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
@using (Html.BeginForm())
{
        <p>
            Find by name: @Html.TextBox("SearchString")  
                <input type="submit" value="Search" /></p>
}
<table class="table">
    <thead>
        <tr>
        <th>
                @Html.ActionLink("Emri", "Index", new { sortOrder = ViewBag.NameSortParm })
        </th>
        <th>
                @Html.ActionLink("Mbiemri", "Index", new { sortOrder = ViewBag.LastnameSortParm })
        </th>
        <th>
                @Html.ActionLink("Email", "Index", new { sortOrder = ViewBag.EmailSortParm })
        </th>
        <th>
                @Html.ActionLink("DataLindjes", "Index", new { sortOrder = ViewBag.DateSortParm })
        </th>
        <th>
                @Html.ActionLink("Role", "Index", new { sortOrder = ViewBag.RoleSortParm })
		</th>
        <th>
				@*<div class="dropdown">
                    <button class="btn btn-secondary dropdown-toggle" type="button" data-toggle="dropdown">
                        Filter by Role
                    </button>
                    <div class="dropdown-menu" id="role-dropdown">
                        <form method="get" action="@Url.Action("Index", "Users")">
                            <select name="selectedRole">
                                <option value="all">All</option>
                                <option value="admin">Admin</option>
                                <option value="moderator">Moderator</option>
                                <option value="user">User</option>
                            </select>
                            <button type="submit">Filter</button>
                        </form>

                    </div>
                </div>*@
                <form method="get" action="@Url.Action("Index", "Users")">
                    <input type="checkbox" name="roles" value="admin" /> Admin
                    <input type="checkbox" name="roles" value="user" /> User
                    <button type="submit">Filter</button>
                </form>

        </th>
		
        
    </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Emri)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mbiemri)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataLindjes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
